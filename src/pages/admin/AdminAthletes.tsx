import { useEffect, useState } from "react";
import { Edit3, Eye, ImagePlus, Plus, Trash2 } from "lucide-react";
import AdminNav from "../../components/AdminNav";
import { supabase } from "../../lib/supabase";
const slugify=(s:string)=>s.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");
const empty={id:"",name:"",slug:"",sport_role:"Arquero profesional",brand:"POKER",excerpt:"",content:"",cover_image_url:"",product_image_url:"",featured:false,status:"draft",sort_order:0};
export default function AdminAthletes(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState<any>(empty);const [open,setOpen]=useState(false);const [saving,setSaving]=useState(false);const load=async()=>{const{data}=await supabase.from("athletes").select("*").order("sort_order");setItems(data??[])};useEffect(()=>{load()},[]);
const upload=async(file:File,key:"cover_image_url"|"product_image_url")=>{const ext=file.name.split(".").pop()||"webp";const path=`atletas/${crypto.randomUUID()}.${ext}`;const{error}=await supabase.storage.from("blog-images").upload(path,file);if(error)return alert(error.message);const{data}=supabase.storage.from("blog-images").getPublicUrl(path);setForm((f:any)=>({...f,[key]:data.publicUrl}))};
const save=async()=>{if(!form.name.trim()||!form.content.trim())return alert("Completa nombre y contenido.");setSaving(true);const payload={name:form.name.trim(),slug:form.slug.trim()||slugify(form.name),sport_role:form.sport_role.trim()||"Atleta",brand:form.brand.trim()||null,excerpt:form.excerpt.trim()||null,content:form.content,cover_image_url:form.cover_image_url||null,product_image_url:form.product_image_url||null,featured:form.featured,status:form.status,sort_order:Number(form.sort_order)||0,published_at:form.status==="published"?(form.published_at||new Date().toISOString()):null,updated_at:new Date().toISOString()};const r=form.id?await supabase.from("athletes").update(payload).eq("id",form.id):await supabase.from("athletes").insert(payload);setSaving(false);if(r.error)return alert(r.error.message);setOpen(false);setForm(empty);load()};const remove=async(id:any)=>{if(!confirm("¿Eliminar este atleta?"))return;await supabase.from("athletes").delete().eq("id",id);load()};
return <main className="min-h-screen bg-zinc-950 px-4 py-8 text-white xl:pl-[15.5rem]"><div className="mx-auto max-w-7xl"><div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.18em] text-[#e3262e]">The House of Sports</p><h1 className="mt-2 text-3xl font-black">Blog de atletas</h1><p className="mt-2 text-sm text-zinc-400">Crea las historias y perfiles de los referentes que trabajan con THS.</p></div><button onClick={()=>{setForm(empty);setOpen(true)}} className="inline-flex items-center gap-2 rounded-xl bg-[#e3262e] px-4 py-3 text-sm font-black"><Plus size={17}/>Nuevo atleta</button></div><AdminNav/>
{open&&<section className="mb-6 rounded-2xl border border-white/10 bg-zinc-900 p-5"><div className="grid gap-3 md:grid-cols-2"><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Nombre del atleta" className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><input value={form.sport_role} onChange={e=>setForm({...form,sport_role:e.target.value})} placeholder="Rol: Arquero profesional" className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><input value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})} placeholder="Marca: POKER / TLSS / 181 KEEPERS" className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><input value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} placeholder="Slug (opcional)" className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><textarea value={form.excerpt} onChange={e=>setForm({...form,excerpt:e.target.value})} placeholder="Resumen corto" className="min-h-24 rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm md:col-span-2"/><div className="flex gap-2"><input value={form.cover_image_url} onChange={e=>setForm({...form,cover_image_url:e.target.value})} placeholder="Foto principal del atleta" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><label className="grid h-[46px] w-[46px] cursor-pointer place-items-center rounded-xl border border-white/10 bg-zinc-950"><ImagePlus size={18}/><input type="file" accept="image/*" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)upload(f,"cover_image_url")}}/></label></div><div className="flex gap-2"><input value={form.product_image_url} onChange={e=>setForm({...form,product_image_url:e.target.value})} placeholder="Foto secundaria / producto" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"/><label className="grid h-[46px] w-[46px] cursor-pointer place-items-center rounded-xl border border-white/10 bg-zinc-950"><ImagePlus size={18}/><input type="file" accept="image/*" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)upload(f,"product_image_url")}}/></label></div><textarea value={form.content} onChange={e=>setForm({...form,content:e.target.value})} placeholder="Historia, trayectoria, relación con THS, productos que usa..." className="min-h-[260px] rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm leading-6 md:col-span-2"/><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})} className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm"><option value="draft">Borrador</option><option value="published">Publicado</option></select><input type="number" value={form.sort_order} onChange={e=>setForm({...form,sort_order:e.target.value})} className="rounded-xl border border-white/10 bg-zinc-950 px-3 py-3 text-sm" placeholder="Orden"/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.featured} onChange={e=>setForm({...form,featured:e.target.checked})}/>Destacado</label></div><div className="mt-4 flex gap-2"><button disabled={saving} onClick={save} className="rounded-xl bg-[#e3262e] px-4 py-2.5 text-sm font-black">{saving?"Guardando...":"Guardar atleta"}</button><button onClick={()=>setOpen(false)} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm font-black">Cancelar</button></div></section>}
<section className="overflow-hidden rounded-2xl border border-white/10 bg-zinc-900"><div className="divide-y divide-white/10">{items.length===0?<p className="p-8 text-center text-zinc-500">Aún no hay atletas.</p>:items.map(a=><article key={a.id} className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3">{a.cover_image_url?<img src={a.cover_image_url} className="h-16 w-20 rounded-lg object-cover"/>:<div className="h-16 w-20 rounded-lg bg-zinc-800"/>}<div><p className="text-[10px] font-black uppercase text-[#e3262e]">{a.brand} · {a.status==="published"?"Publicado":"Borrador"}</p><h3 className="mt-1 font-black">{a.name}</h3><p className="text-xs text-zinc-500">{a.sport_role}</p></div></div><div className="flex gap-2"><a href={`/atletas/${a.slug}`} target="_blank" className="grid h-9 w-9 place-items-center rounded-lg border border-white/10"><Eye size={16}/></a><button onClick={()=>{setForm({...empty,...a});setOpen(true)}} className="grid h-9 w-9 place-items-center rounded-lg border border-white/10"><Edit3 size={16}/></button><button onClick={()=>remove(a.id)} className="grid h-9 w-9 place-items-center rounded-lg border border-red-500/20 text-red-400"><Trash2 size={16}/></button></div></article>)}</div></section></div></main>}
